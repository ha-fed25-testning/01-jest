# 01-jest

Kodexempel på enhetstest med Jest.

Skapa projekt:

```bash
mkdir project-name       <- skapa projektmapp
cd project-name/
git init
npm init -y              <- skapa package.json
npm install --save-dev jest
npm create jest@latest   <- skapa Jest config-fil
```
